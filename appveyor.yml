# https://www.appveyor.com/docs/build-environment/#build-worker-images
image: Visual Studio 2017

build: false

environment:
  debug_artifacts: '_debug'

test_script:
  - ps: |
      "PSVersionTable:"
      $PSVersionTable
      "`n"
      "PSModulePath:"
      $ENV:PSModulePath -split ';'
      "`n"
      "OS:"
      Get-WMIObject win32_operatingsystem | Select Caption, Version

      $null = mkdir "$ENV:debug_artifacts" -Force
      Get-Variable | Export-CliXml -Path "$ENV:debug_artifacts\Variables.xml" -Depth 3
      Get-Module | Export-CliXml -Path "$ENV:debug_artifacts\LoadedModules.xml" -Depth 3
      Get-Module | Export-CliXml -Path "$ENV:debug_artifacts\AllModules.xml" -Depth 3
      Get-ChildItem "ENV:" | Export-CliXml -Path "$ENV:debug_artifacts\Env.xml" -Depth 3
      Get-PackageProvider | Export-CliXml -Path "$ENV:debug_artifacts\PackageProvider.xml" -Depth 3
      Get-PackageSource | Export-CliXml -Path "$ENV:debug_artifacts\PackageSource.xml" -Depth 3

artifacts:
  - path: "$(debug_artifacts)"